dependencies:
  # The new I2C driver, driver/i2c_master.h, arrived in 5.2.
  idf:
    version: ">=5.2"
  # The component lives in ../components/i2console in this repository, so the
  # component manager finds it locally and no registry entry is needed.
